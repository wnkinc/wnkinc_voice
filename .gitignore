node_modules/
cdk.out/
dist/
*.log
.DS_Store
# Tenant rows are data, not code: only the template is tracked
tenants/*
!tenants/example.json

# Third-party repos cloned for study, not part of the build
vendor/
tsconfig.tsbuildinfo
